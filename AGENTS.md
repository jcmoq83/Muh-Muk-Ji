# AGENTS.md

이 문서는 이 저장소에서 Codex가 매일 작업할 때 참조하는 운영 메모다.
목표는 "매번 같은 설명을 반복하지 않고" 일관된 방향으로 개발/배포하는 것이다.

## 1) 프로젝트 방향
- 이 프로젝트는 일일 바이브 코딩 기반으로 빠르게 개선한다.
- 작은 단위 변경을 자주 반영하고, 바로 배포 가능한 상태를 유지한다.
- 기능 추가보다 사용자 체감 품질(버그 감소, 흐름, 안정성)을 우선한다.

## 2) 기본 준수 사항
- 기존 동작을 깨는 대규모 변경은 피하고, 필요하면 단계적으로 적용한다.
- 변경 파일은 최소화하고, 이유가 명확한 수정만 반영한다.
- 커밋 메시지는 작업 의도가 드러나게 작성한다.
- 배포 전 최소 동작 확인(페이지 로드, 핵심 인터랙션, 콘솔 에러) 수행.
- 긴급 수정이 아니면 로그를 남긴 뒤 배포한다.

## 3) 기본 작업 루틴 (매일)
1. 최근 변경 파악 (`git status`, 최근 커밋/파일 확인)
2. 오늘 작업 목표를 `docs/daily-log/YYYY-MM-DD.md` 상단에 기록
3. 구현/수정
4. 로컬 확인(기본 동작/오류)
5. 커밋
6. GitHub 반영
7. Cloudflare 배포 확인
8. 일일 로그에 결과/이슈/다음 액션 정리

## 4) 배포 루틴 (GitHub -> Cloudflare)
1. 작업 브랜치 또는 `main`에서 변경 반영
2. `git add -A`
3. `git commit -m "<type>: <summary>"`
4. `git push`
5. GitHub 원격 반영 확인
6. Cloudflare(Workers/Pages) 배포 상태 확인
7. 배포 URL에서 최종 확인

## 5) 커밋 메시지 가이드
- 권장 형식: `<type>: <summary>`
- 예시 type: `feat`, `fix`, `refactor`, `chore`, `docs`, `style`
- 예시: `fix: tournament result modal close bug`

## 6) 로그 운영 규칙
- 위치: `docs/daily-log/`
- 파일명: `YYYY-MM-DD.md` (UTC 기준 날짜)
- 매일 최소 1개 로그 작성
- 아래 항목은 가능하면 항상 채운다:
  - 오늘 목표
  - 실제 변경 사항
  - 배포/검증 결과
  - 이슈/리스크
  - 다음 작업

## 7) Codex 실행 원칙
- Codex는 작업 시작 시 `AGENTS.md`와 가장 최근 `docs/daily-log/*.md`를 먼저 확인한다.
- 기존 방향과 충돌하는 요청이 있으면, 충돌 지점을 먼저 짚고 진행한다.
- 불확실한 내용은 로그에 "가정(assumption)"으로 남긴다.
