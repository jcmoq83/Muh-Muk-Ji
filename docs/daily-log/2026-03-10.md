# Daily Log - 2026-03-10

## 오늘 목표
- 수동 토너먼트의 `저칼로리 순으로` 옵션을 `건강식 위주`로 바꾸고, 설명 툴팁과 새 건강식 기준 순위를 반영

## 작업 내용
- `recipe.html` 자동 생성형 레시피 146개를 폐기하고, 실제로 레시피로 성립하는 메뉴 50개만 남기는 검수형 큐레이션 구조로 전면 교체
- 삼겹살, 소고기 등 단순 굽기형과 검수 미완료 메뉴는 `보류 중인 메뉴`로 분리
- 상단 내비의 `메뉴 토너먼트` 버튼 라벨을 `머먹지!?`로 변경하고, 같은 레벨에 `레시피` 버튼을 추가
- `recipe.html` 정적 허브 페이지를 추가해 현재 토너먼트 메뉴 146개를 카테고리/메뉴군/검색 기준으로 탐색 가능한 레시피 카드 UI로 정리
- `site-pages.css`, `sitemap.xml`을 갱신해 새 레시피 페이지 디자인과 색인을 반영
- `privacy.html` 히어로 영역의 `메뉴 토너먼트 바로 시작`, `메뉴 사전 둘러보기` CTA를 제거
- 사이트 전체 상단 내비에서 `홈` 링크를 제거
- `about.html`, `contact.html` 히어로 영역의 `메뉴 토너먼트 바로 시작`, `메뉴 사전 둘러보기` CTA를 제거
- `about.html`에서 `왜 토너먼트가 메인인가`, `콘텐츠는 왜 필요한가` 소개 카드 2개를 제거
- 사이트 전반 공통 상단 내비에서 `메뉴 사전`, `메뉴 비교`, `상황별 추천`, `건강식 기준` 링크를 제거
- 홈 푸터의 동일 링크 4개와 관련 소개 문구를 현재 구조에 맞게 정리
- 홈 `index.html`에서 `메뉴 사전에서 많이 보는 항목`, `많이 보는 메뉴 비교`, `상황별 추천`, `건강식 기준은 이렇게 봅니다`, `추천 읽을거리` 지원 섹션 5개를 제거
- `index.html` 수동 토너먼트 옵션 라벨을 `건강식 위주`로 변경
- 옵션 hover/focus 시 `저칼로리, 저탄고지, 영향균형 등 반영` 툴팁이 뜨도록 UI 추가
- 건강식 우선 정렬 배열을 요청한 순서 기준으로 전면 교체
- 건강식 정렬 활성화 토스트 문구를 `건강식 위주 정렬을 적용했어요`로 조정
- 건강식 체크를 해제하면 메뉴판이 다시 랜덤 순서로 한 번 섞이고, 해제 상태에서는 그 순서를 유지하도록 로직 수정
- 건강식 토글 on/off 때 1군~3군의 기존 선택 개수는 유지하면서, 선택된 메뉴 자체를 현재 모드 기준(건강식 우선 / 랜덤)으로 다시 뽑도록 재동기화 로직 추가
- `사용 방법` 카드는 웹에서 기존처럼 좌우 카드 전체 높이를 유지하되, 내부 헤더 패딩과 제목 여백만 줄여 상하 공간이 글자 크기에 더 맞도록 보정
- 애드센스 신청 준비를 위해 메인 페이지에 서비스 소개, 건강식 기준 안내, 운영자/문의 정보, 공통 푸터 링크를 추가
- `about.html`, `how-it-works.html`, `privacy.html`, `contact.html` 정적 페이지를 추가해 소개/이용안내/정책/문의 문서를 분리
- `robots.txt`, `sitemap.xml`, `ads.txt`(퍼블리셔 ID 입력 전 안내용) 파일을 추가
- 정적 페이지 공통 스타일 파일 `site-pages.css`를 추가
- 모바일에서 토너먼트 시작 시 이전 설정 스크롤 위치와 무관하게 `VS` 중심으로 자동 스크롤해 두 메뉴가 함께 보이는 지점으로 이동하도록 수정
- 홈 상단에 크롤링 가능한 글로벌 내비를 추가하고, 토너먼트 아래에만 소개/사용법/메뉴 사전/메뉴 비교/상황별 추천/건강식 기준/하단 CTA 섹션을 배치해 홈을 여전히 토너먼트 우선 구조로 유지
- `scripts/generate_content_pages.py` 생성 스크립트를 추가해 메뉴 사전, 메뉴 비교, 상황별 추천, 건강식 기준, 허브 페이지, sitemap을 일괄 생성 가능하게 구성
- 메뉴 사전 28개, 메뉴 비교 12개, 상황별 추천 10개, 허브 4개를 추가해 전체 HTML 페이지 수를 59개로 확장
- 모든 생성 페이지에 고유 title, meta description, canonical, H1, WebSite/BreadcrumbList/Article 구조화 데이터를 추가
- 내부 링크 유효성 검사를 통과하도록 허브와 세부 페이지 간 링크 구조를 정리

## 테스트/검증
- [x] `recipe.html` 레시피 수를 50개로 축소하고 보류 메뉴 영역 분리 확인
- [x] `recipe.html`에서 자동 추론형 레시피 문구 제거 확인
- [x] 상단 내비에서 `머먹지!?`, `레시피`, `소개`, `문의` 구조 반영 확인
- [x] `recipe.html` 생성 및 레시피 카드 146개 렌더 기준 반영 확인
- [x] `recipe.html`의 검색/카테고리/메뉴군 필터 스크립트 연결 확인
- [x] `sitemap.xml`에 `recipe.html` URL 추가 확인
- [x] `privacy.html` CTA 제거 확인
- [x] 사이트 전체 상단 내비에서 `홈` 링크 미노출 확인
- [x] `about.html`, `contact.html` CTA 제거 확인
- [x] `about.html` 소개 카드 2개 제거 확인
- [x] 공통 상단 내비에서 제거 대상 4개 링크 미노출 확인
- [x] 홈 푸터 링크와 소개 문구 정리 반영 확인
- [x] 홈 `support-home` 섹션에서 요청한 5개 카드 블록 제거 확인
- [x] 인라인 스크립트 문법 검사
- [x] 건강식 순위 배열이 현재 기본 메뉴 집합을 모두 커버하는지 확인
- [x] 렌더 DOM에서 `건강식 위주` 라벨과 툴팁 문구 반영 확인
- [x] 건강식 체크 해제 시 랜덤 재섞기 로직과 기본 정렬 분기 반영 확인
- [x] 건강식 토글 재체크 시 1군~3군 선택 메뉴를 현재 기준으로 다시 뽑는 로직 반영 확인
- [x] 사용 방법 카드 높이 축소 스타일과 모바일 자동 스크롤 함수 반영 확인
- [x] 메인 페이지 렌더 DOM에서 서비스 소개/운영자 정보/정책 링크 노출 확인
- [x] 정적 페이지 4개와 색인 파일(`robots.txt`, `sitemap.xml`, `ads.txt`) 생성 확인
- [x] 생성된 전체 HTML 59개, sitemap URL 59개 일치 확인
- [x] 내부 링크 누락 0건 확인
- [x] 카테고리별 생성 수 확인 (`dictionary` 29, `compare` 13, `situations` 11, `healthy-criteria` 1)
- [x] 홈 렌더 DOM에서 글로벌 내비와 지원 섹션(`메뉴 사전`, `메뉴 비교`, `상황별 추천`, `추천 읽을거리`) 노출 확인
- [ ] 배포 후 실제 동작 확인

## 배포 기록
- Git commit: 예정
- Git push: 예정
- Cloudflare 배포: GitHub push 후 확인 예정
- 배포 URL: 확인 예정

## 이슈/리스크
- `영향균형` 문구는 사용자 요청 문구를 그대로 반영함
- 건강식 순위 배열에는 현재 기본 메뉴 외의 과거/별칭 메뉴도 일부 포함되어 있어, 추가 메뉴가 다시 활성화돼도 같은 기준을 재사용할 수 있음
- `ads.txt`에는 실제 AdSense 퍼블리셔 ID가 없어 안내용 주석만 넣었고, 승인 후 `ca-pub-...` 값으로 교체해야 함

## 다음 작업
- 배포본에서 홈 지원 섹션 축소가 의도대로 반영됐는지 확인
