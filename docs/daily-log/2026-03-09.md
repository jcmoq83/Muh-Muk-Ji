# Daily Log - 2026-03-09

## 오늘 목표
- 티어별 비중 조절 슬라이더와 수동 선택 상태를 일관되게 동기화

## 작업 내용
- `index.html`의 티어 선택 상태 관리를 단순화
- 슬라이더 조절 시 해당 티어의 실제 선택 수에 맞춰 랜덤 추가/제거되도록 수정
- 메뉴를 직접 선택/해제할 때 티어 슬라이더 값과 개수 표시가 즉시 따라오도록 수정
- 파스타 메뉴 4종(`오일파스타`, `까르보나라`, `토마토파스타`, `로제파스타`)을 `파스타` 1종으로 통합
- `파스타` 메뉴 이미지를 `images/토마토파스타.png`로 연결
- 이미지 자동 동기화에서 기존 파스타 파일명이 `기타 메뉴`로 다시 생기지 않도록 alias 처리
- 설정 화면에서 `자동 토너먼트` / `수동 토너먼트` 구분이 더 눈에 들어오도록 제목, 설명, 강조 박스, 모바일 순서 조정
- 그룹 라벨 표기를 `1군 메뉴 (56개)` 형식으로 통일
- 자동/수동 토너먼트 카드의 헤더 레벨을 맞추고, 보조 설명 박스 없이 디자인만으로 구분되도록 정리
- 수동 토너먼트 카드 헤더에 안내 문구를 직접 넣어 자동 카드와 같은 헤더 레벨에서 읽히도록 조정
- 수동 카드 헤더 문구를 축약하고, 자동/수동 카드 타이포 레벨과 상단 포인트 컬러를 다시 정리
- `파스타` 메뉴 라벨이 줄바꿈되지 않고 한 줄로 표시되도록 조정
- 자동/수동 카드 보조 문구가 동일한 두께와 줄높이로 보이도록 타이포를 다시 고정
- 플레이 화면의 `VS` 카드를 진행률 카드로 확장해 모바일/웹에서 직관적으로 진행 상태를 표시하도록 수정
- `VS` 카드에 현재 강수와 진행 수치를 함께 표시하고, 강수별로 진행 바 색상이 바뀌도록 조정
- 모바일 플레이 카드와 `VS` 카드 높이를 전반적으로 축소

## 테스트/검증
- [x] `index.html` 인라인 스크립트 문법 검사 통과
- [x] 헤드리스 Chromium으로 렌더 DOM 확인 (`1군 56`, `2군 49`, `3군 40`, `기타 메뉴` 없음)
- [x] 헤드리스 Chromium으로 UI 문구/그룹 라벨 확인 (`자동 토너먼트`, `수동 토너먼트`, `1군 메뉴 (56개)` 등)
- [x] 모바일 폭 스크린샷 생성으로 패널 강조 및 상단 배치 흐름 확인
- [x] 자동/수동 카드에서 보조 설명 박스 제거 및 동일 헤더 레벨 유지 확인
- [ ] 배포 후 실제 동작 확인
- 메모: UI 상호작용은 브라우저에서 추가 확인 필요

## 배포 기록
- Git commit: `433bbe5` (`style: refine versus card progress and mobile sizing`)
- Git push: `origin/main` 반영 완료
- Cloudflare 배포: GitHub push로 트리거됨
- 배포 URL: 확인 예정

## 이슈/리스크
- 슬라이더 감소 시 해제 대상은 현재 선택된 메뉴 중 무작위로 결정됨

## 다음 작업
- 브라우저에서 티어 슬라이더/수동 선택 조합 흐름 재검증
